[project]
name = "todo-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.0.0",
    "python-escpos>=3.1",
    "signalbot>=0.20.0",
]
